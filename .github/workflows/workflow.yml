'on':
  push:
    branches:
    - main
jobs:
  dev:
    runs-on: self-hosted
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Copy to Airflow
      run: sudo cp -r dags/sample_dag.py /home/ec2-user
  qa:
    runs-on: self-hosted
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Copy to Airflow
      run: sudo cp -r dags/sample_dag.py /home/ec2-user
